services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - repo:/app/repo
      - ~/.ssh:/home/nextjs/.ssh:ro
    restart: unless-stopped

volumes:
  repo:
